# Задача: Verilog-CAD калькулятор — Фаза 0 (стабилизация round-trip)

Привет! Передаю тебе задачу по «умному калькулятору» с CAD-холстом. Здесь всё, что нужно, чтобы войти в контекст и довести работу до конца без блужданий. Читай по порядку, не пропускай шаги.

---

## TL;DR (если совсем коротко)

Мы рисуем электрические схемы на SVG-холсте и сериализуем их в текстовый Verilog-подобный язык (и обратно). Главный закон системы: **round-trip без потерь** — `parse(export(draft)) ≡ draft`. Сейчас он нарушен в 5 местах. Твоя задача — починить их (Фаза 0 из ТЗ, задачи F0.1–F0.7). Каждый фикс уже имеет готовый воспроизводящий тест.

---

## Шаг 1. Прочитай документы (в этом порядке, ~1 час)

1. **`AGENTS.md`** (корень репо) — жёсткие правила проекта: Next.js 16, `'use cache'`, Prisma только из `@/lib/prisma`, Zod без `.cuid()`, Tailwind v4 без конфига. Нарушение любого пункта = PR не пройдёт ревью.
2. **`docs/tz/TZ_VERILOG_CAD_AGENT.md`** — главный документ:
   - **Часть 0** — аудит: таблица дефектов D1–D9 с указанием файлов и строк. Это твой список багов.
   - **Часть I** — теория (зачем Verilog, что такое EDL-V). Прочитай для понимания «куда всё идёт», но в Фазе 0 ты её НЕ реализуешь.
   - **Часть II, раздел «Фаза 0»** — твои задачи F0.1–F0.7 с критериями приёмки.
3. **`calculator_cad_concept.md`** — общая концепция CAD-холста (контекст продукта).

## Шаг 2. Запусти то, что уже работает (~15 минут)

```powershell
cd C:\Users\sevri\Сайт\elektronom
npx tsx scripts/test-verilog.ts          # базовые тесты: должно быть 32/32 PASS
npx tsx scripts/test-verilog-edge-cases.ts  # адверсариальные кейсы: здесь видны баги
npx tsc --noEmit                          # типы: должно быть чисто
```

Второй скрипт — твой главный инструмент: каждый его кейс №1–№8 соответствует дефекту из аудита. Сейчас он печатает неправильные значения (например, кейс №3: позиция (10,1) превращается в (1,1)). **Когда все 8 кейсов печатают правильные значения — Фаза 0 готова.**

## Шаг 3. Изучи код, который будешь менять (~1 час)

Весь стек — 5 файлов в `src/lib/engineering/verilog/`:

| Файл | Что делает | Какие дефекты здесь |
|---|---|---|
| `verilog-exporter.ts` | draft → текст Verilog | D3 (не пишет earthing), D4 (не пишет route), D6, D7 |
| `verilog-parser.ts` | текст → AST | D6, D7, D8 |
| `verilog-interpreter.ts` | AST → draft | D2 (price=0), D5, D6 |
| `verilog-auto-layout.ts` | расстановка узлов без координат | **D1 — самый важный: затирает явные координаты** |
| `index.ts` | pipeline целиком | — |

Смежное (читать, не менять): `graph.ts` (типы), `cae.ts`, `normguard/`, холст `src/components/engineering/engineering-drawing-designer.tsx` (строки 528–563 — как UI вызывает экспорт/компиляцию).

## Шаг 4. Порядок работы — по одной задаче за раз

Делай строго в этом порядке (каждая следующая опирается на предыдущую):

1. **F0.1** (D1) — авто-layout не трогает узлы с явными координатами. Критерий: кейс №3 сохраняет (10,1).
2. **F0.2** (D3) — `earthing`, `locale`, `name`, `phase` сериализуются в шапку. Критерий: кейс №5, TT → TT.
3. **F0.3** (D4) — экспорт `// route:` для waypoints. Критерий: кейс №6.
4. **F0.4** (D2, D5) — price подтягивается из existingDraft/каталога. Критерий: кейс №4, 350 → 350.
5. **F0.5** (D6) — `conductor: 'power'` больше не попадает в граф. Критерий: кейс №2, `undefined`.
6. **F0.6** (D7, D8) — `DC+`→`DCp`/`DC-`→`DCm` в именах wire; инстанс без `#()` = узел или явная ошибка. Критерий: кейсы №1, №7.
7. **F0.7** — перенос edge-cases в vitest как настоящие assert-ы + property-тест round-trip.

**Правила для каждой задачи:**
- Одна задача = одна ветка = один PR. Название ветки: `fix/verilog-f0-1-autolayout` и т.п.
- Перед фиксом — добавь falling assert в тест (сейчас скрипт только печатает), убедись что он красный, почини, убедись что зелёный. Это classic red-green.
- После каждого фикса прогоняй ВСЕ ТРИ команды из Шага 2 — старые 32 теста не должны сломаться.
- В описании PR: какой дефект (D-номер), какой тест-кейс подтверждает, что НЕ менялось.

## Шаг 5. Чего НЕ делать (это важно)

- ❌ Не переписывай язык на «порты и атрибуты» (это Фаза 1, не твоя зона сейчас) — даже если очень хочется.
- ❌ Не трогай NormGuard, BOM, калькулятор (`calculators.ts`), Prisma-схему, Algolia.
- ❌ Не меняй формат экспорта так, что старые сохранённые черновики перестанут парситься (в localStorage у пользователей лежат v2.1-источники).
- ❌ Не добавляй зависимости без согласования.
- ❌ Если застрял больше чем на 2 часа — не молчи. Напиши: что пробовал, что ожидал, что получил, минимальный воспроизводящий пример.

## Definition of Done всей Фазы 0

- [ ] Все 8 кейсов `test-verilog-edge-cases.ts` дают корректные значения (и переведены в vitest с assert-ами)
- [ ] Старые 32 теста зелёные
- [ ] `npx tsc --noEmit` чистый
- [ ] Property-тест: 1000 случайных draft проходят `interpret(parse(export(d))) ≡ d`
- [ ] На холсте (`/uk/calculators`) вручную проверено: нарисовал схему → скопировал Verilog → очистил → вставил → compile → схема идентична, включая позиции и провода

## Вопросы

Сначала ищи ответ в ТЗ (там есть Приложение А «дефект → фаза» и раздел «Риски»). Если ответа нет — спрашивай сразу, плохой вопрос дешевле плохого коммита. Чекпоинт: короткий статус раз в день — «сделал X, делаю Y, блокеров нет/есть такой-то».
