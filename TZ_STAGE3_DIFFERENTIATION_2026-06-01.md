# ТЗ — Стадия 3: Дифференциация / «ров» (спринт-уровень)

> Дата: 2026-06-01. Развёртка Стадии 3 из `TZ_ENTERPRISE_ROADMAP`. Это **то, чего нет у конкурентов на Prom** — главный рычаг «лучший в сегменте».
> **Предусловие:** Стадия 1 (тесты/observability) и Стадия 2 (очереди E2.6 для эмбеддингов/синка, платежи E2.1 для B2B-счетов) закрыты.
> Правила: `MASTER_CONTEXT`+addendum, §0 anti-regression. ⚠️ **Изменения схемы (Company, QuoteRequest, vector, qty_breaks) — санкционировать в `MASTER_CONTEXT §6`** отдельным addendum (как делали с Google Shopping), затем миграция (A-8).
> Источник стратегии: `_reversa_sdd/vision-next-level.md` (Ставки 1–3). Опора: модели `TechnicalDocument/DocumentChunk/AssistantSession`, `SupplierInventory`, регидратация AI из БД (сохранить!).

---

## E3.1 — AI-советник v2 🌟 (killer-feature)
Превратить keyword-демо в настоящего инженера-консультанта.

### T3.1.1 Векторный RAG (pgvector)
- Включить расширение `pgvector` в Postgres. `DocumentChunk.embedding`: `String`(JSON) → нативный `vector(1536)` (под выбранную модель эмбеддингов).
- ⚠️ Prisma не поддерживает `vector` нативно — `Unsupported("vector(1536)")` + similarity через `$queryRaw` (оператор `<=>`). Индекс HNSW/IVFFlat:
  ```sql
  CREATE INDEX ON document_chunks USING hnsw (embedding vector_cosine_ops);
  ```
- **Приёмка:** поиск по близости через `$queryRaw` возвращает top-k чанков за <50мс на 100k+ чанков.

### T3.1.2 Пайплайн эмбеддингов (через очередь E2.6)
- Провайдер эмбеддингов: OpenAI `text-embedding-3-small` / Voyage / Cohere (Anthropic эмбеддинги не делает). Ключ в `env.ts`.
- Job `assistant/embed-docs`: `TechnicalDocument` → чанкинг (≈500–800 токенов, overlap) → эмбеддинг → запись в `DocumentChunk`. Ре-эмбеддинг при изменении документа.
- **Приёмка:** загрузка техдока → автоматически появляются чанки с эмбеддингами; идемпотентно.

### T3.1.3 Retrieval + реальные источники
- В `lib/assistant/claude.ts`: заменить keyword-дамп каталога на **RAG-поиск** (эмбеддинг запроса → top-k чанков) + товары; `sources` — **реальные документы** (title/url/snippet), убрать плейсхолдеры.
- **Приёмка:** ответ ссылается на реальные источники из `TechnicalDocument`; нет хардкод-«ДСТУ».

### T3.1.4 Tool-use (официальный SDK)
- Мигрировать с raw `fetch` на `@anthropic-ai/sdk`; определить tools: `searchProducts`, `getStock`, `getCompatible`, `ragSearch`. Модель **вызывает инструменты**, а не получает дамп в промпт.
- **Сохранить регидратацию из БД** (цена/наличие — не от LLM). Фильтры prompt-injection, лимиты, учёт стоимости — сохранить.
- **Приёмка:** ассистент дёргает инструменты; цены/наличие из БД; стриминг ответа.

### T3.1.5 Сборка комплекта (BOM) — «собрать щит» ⭐
- Сценарий/инструмент: вход (тип объекта, кол-во групп/мощность) → расчёт состава щита (ввод/реклоузер, **УЗО на группы**, автоматы по группам, бокс на N модулей) по доменным правилам электрики → структурированный BOM → **«добавить всё в корзину»**.
- Доменные правила вынести в `lib/assistant/electrical-rules.ts` (номиналы, селективность, УЗО 30мА на розеточные группы и т.п.).
- **Приёмка:** «щит на 2-комн.» → валидный комплект (совместимый, с расчётом) в корзину одной кнопкой.

### T3.1.6 Инженерные калькуляторы
- `lib/calc/`: сечение кабеля (по мощности/длине/падению напряжения), номинал автомата, автономия ИБП (ёмкость×напряжение×КПД / нагрузка).
- Standalone-страницы `(content)/calculators/*` (лид-магниты + SEO) + как tools ассистента.
- **Приёмка:** калькуляторы дают корректные значения (проверка по эталонным примерам); индексируются.

---

## E3.2 — B2B по-взрослому 💼 (самая маржинальная аудитория — профи)

### T3.2.1 Профиль юр.лица
- Модель `Company { id, userId, name, edrpou, vatNumber?, billingAddress Json, isVatPayer Boolean, creditLimit Decimal?, paymentTerms Int? }`; тип аккаунта B2B; связь с `User`/`Order`.
- **Приёмка:** пользователь создаёт компанию (ЄДРПОУ/реквизиты), привязывает к заказам.

### T3.2.2 Счёт с ПДВ + оплата по рахунку
- Генерация **рахунок-фактура (PDF)** с ПДВ (20%); метод оплаты «оплата на рахунок» (deferred, без онлайн-эквайринга).
- **Приёмка:** юр.лицо получает корректный счёт с ПДВ; заказ помечен «оплата по рахунку».

### T3.2.3 RFQ «Запрос прайса»
- Модель `QuoteRequest { id, userId?, items Json, status QuoteStatus, quotedTotal?, validUntil? }`, enum `REQUESTED|QUOTED|ACCEPTED|REJECTED`; форма-воронка; админ котирует; конверсия в заказ.
- **Приёмка:** клиент шлёт RFQ → админ котирует → клиент принимает → заказ.

### T3.2.4 Заказ списком / CSV по артикулам
- UI уже намекает (`searchByMpn`, `skuPlaceholder`). Парсер списка `SKU/MPN — кол-во` → массовое добавление в корзину с валидацией наличия; ре-ордер прошлого заказа.
- **Приёмка:** вставка списка артикулов формирует корзину; невалидные подсвечены.

### T3.2.5 Кредит-лимиты / условия
- `Company.creditLimit`/`paymentTerms`; заказ на условиях (net-30); контроль лимита.
- **Приёмка:** заказ в кредит в пределах лимита; превышение блокируется.

### T3.2.6 Опт-цены / прайс-листы
- Довести `qty_breaks` до **схемы** (сейчас расхождение mock↔код, см. C-4/P-2); B2B-ценовые ступени; выгрузка прайс-листа (PDF/CSV) для компании.
- **Приёмка:** опт-цена применяется по кол-ву; прайс-лист выгружается.

---

## E3.3 — Инвентарь-хаб 🔗 (ваш ров на базе robot-экосистемы)

### T3.3.1 Синхронизация поставщиков в приложении
- Узаконить supplier sync как **server action/job** (очередь E2.6) или сервис: импорт `SupplierInventory` из фидов ASKO/Prom (легитимизировать `C:\робот`-скрипты в приложение/мост). Мульти-поставщик (`supplierName`).
- Правила: не перезаписывать `Product.price` вслепую — markup-правила; обновлять `stock`/срок поставки.
- **Приёмка:** остатки/цены поставщиков обновляются процессом приложения (не вручную/внешне), идемпотентно.

### T3.3.2 Честная доступность
- Из `SupplierInventory` выводить «**в наявності / під замовлення N днів**» вместо бинарного `stock>0`; показывать на карточке/в каталоге; в фиде `availability=backorder` (уже реализовано в `feed-builder`).
- **Приёмка:** карточка показывает срок поставки; меньше отмен «нет в наличии».

### T3.3.3 Мост с robot-экосистемой
- Связка ASKO/Prom/Sheets ↔ сайт (вебхук/импорт-эндпоинт); единый «инвентарь-хаб».
- **Приёмка:** изменение остатков в источнике отражается на сайте в пределах SLA синка.

---

## Definition of Done (Стадия 3)
- [ ] RAG векторный: эмбеддинги/поиск работают, источники реальные; tool-use; регидратация сохранена.
- [ ] «Собрать щит» даёт валидный BOM в корзину; калькуляторы корректны.
- [ ] B2B: счёт с ПДВ, RFQ-цикл, заказ списком, кредит-лимиты, опт-цены.
- [ ] Инвентарь: синк поставщиков в приложении, честная доступность, мост с robot-экосистемой.
- [ ] Схемные изменения санкционированы в `MASTER_CONTEXT §6` (addendum) + миграции; `lint`/`tsc`/`test`/`build` зелёные.

## Зависимости / трассировка
- Зависит от: E2.6 (очереди — эмбеддинги/синк), E2.1 (платежи — B2B-счета/кредит), Стадия 1 (тесты).
- Schema-изменения (Company/QuoteRequest/vector/qty_breaks) → **addendum к §6** (по образцу `MASTER_CONTEXT_v1_3_ADDENDUM_GOOGLE_SHOPPING`).
- Связано: `_reversa_sdd/vision-next-level.md` (Ставки 1–3), `TZ_DEV_FEATURE_SPEC` (assistant/suppliers), `_reversa_sdd/data-dictionary.md`.
