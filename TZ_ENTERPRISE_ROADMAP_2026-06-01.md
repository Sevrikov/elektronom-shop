# ТЗ — Дорожная карта до enterprise / «лучший в сегменте» (постадийно)

> Дата: 2026-06-01. Зонтичный план над частными ТЗ. Цель: довести Elektronom до уровня топового enterprise-магазина.
> Слои-источники: `TZ_FIXES_MASTER`, `TZ_SEO_FIXES_ROUND2`, `TZ_SEO_TOP10`, `TZ_GOOGLE_SHOPPING` (+addendum), `TZ_DEV_FEATURE_SPEC`, `_reversa_sdd/vision-next-level.md`.
> **Сквозные правила (на каждой задаче):** соблюдать `MASTER_CONTEXT` + addendum; §0 anti-regression; перед сдачей `lint`/`tsc --noEmit`/`build` зелёные; без `any` (§3.1), `select`+`take` (§3.3), Tailwind без inline-`style` (§10), `next/image` (§17), тексты через `next-intl` (§12).

## Принцип
Стадии 0–1 — **входной билет** (без них «enterprise» невозможен). 2 — масштаб+коммерция. 3 — ров (дифференциация). 4 — авторитет. Не перепрыгивать: каждая стадия закрывается по Exit-критериям (KPI/SLA) перед следующей.

---

# СТАДИЯ 0 — Закрыть текущий долг (1–2 недели)
**Цель:** чистая база. **Источник:** `TZ_FIXES_MASTER` + `TZ_SEO_FIXES_ROUND2`.
- Бренд → `Electronom`; sitemap `take:200`→все товары; XSS-санитайз описаний; убрать `any`/inline-`style`; `gtin/mpn` прогон; ru/uk паритет ключей.
- **Exit:** все P0 из round-2 закрыты; `build` зелёный; нет дублей ключей; sitemap полный.

---

# СТАДИЯ 1 — Операционная зрелость (фундамент топа)
**Цель:** надёжность и скорость уровня enterprise. **Exit/SLA:** CI-гейт зелёный; ошибки трекаются; CWV зелёные (mobile p75); пулинг включён; security-заголовки есть.

### E1.1 Тесты + CI/CD 🔴
- Vitest (unit/integration) + Playwright (e2e), `tests/{unit,integration,e2e}`.
- Покрыть критпуть: `createOrder` (tx/идемпотентность/сток), паритет фасетов SQL↔in-memory, `mergeCartIfNeeded`, RBAC, отзыв, фид (`feed-builder`).
- CI `.github/workflows/ci.yml`: `install→lint→tsc→vitest→build` (+ Playwright на ephemeral Postgres). Гейт на PR.
- **Приёмка:** PR не мёржится при красных тестах; критпуть покрыт; smoke e2e зелёный.

### E1.2 Connection pooling (Vercel) 🔴
- Prisma Accelerate **или** PgBouncer; `DATABASE_URL` (pooled) + `DIRECT_URL` (миграции) в `schema.prisma`/`env.ts`/`.env.example`.
- **Приёмка:** под нагрузкой нет `too many connections`; миграции идут через `DIRECT_URL`.

### E1.3 Observability 🔴
- **Sentry** (ошибки фронт+бэк) через `instrumentation.ts`.
- **OpenTelemetry** трейсинг (instrumentation.ts уже есть — достроить экспорт); структурные логи (`lib/logger`) → агрегатор.
- **Uptime/CWV-мониторинг** (Vercel Analytics/CrUX + алерты). Дашборд: ошибки, latency, заказы, AI-стоимость.
- **Приёмка:** ошибки прод видны в Sentry с трейсами; алерты на 5xx/деградацию CWV.

### E1.4 Security hardening 🟡
- **CSP** и security-заголовки (`next.config`/middleware-proxy): `Content-Security-Policy`, `X-Content-Type-Options`, `Referrer-Policy`, HSTS.
- Санитайз HTML описаний (на входе admin + на выводе) — `lib/sanitize.ts`.
- Вынести **IP из открытого `AssistantMessage.structured`** (PII); rate-limit перенести (см. E2.7).
- Включить **роль MANAGER** для подмножества admin + **аудит-лог** админ-действий (модель `AuditLog`).
- ENV-валидация полная (`ANTHROPIC_API_KEY`, `CONTENT_FACTORY_*`, payment, merchant).
- **Приёмка:** CSP без нарушений; XSS-payload вырезается; аудит фиксирует кто/что/когда; secret-scan чистый.

### E1.5 Производительность baseline 🟡
- **Серверный image-pipeline:** удаление фона — Cloudinary AI (не client-canvas `TransparentImage`); хранить готовые PNG; **хостить изображения у себя/Cloudinary** (не хотлинк `images.prom.ua`).
- `next/image` везде (вкл. `hero-carousel`), `priority` на LCP-элементе, `sizes`.
- Убрать тяжёлый JS из above-the-fold (баннеры на CSS/`requestAnimationFrame`).
- **Приёмка:** INP(p75)<200мс, LCP<2.5с, CLS<0.1 (mobile, CrUX); нет client-canvas на главной; нет хотлинков.

---

# СТАДИЯ 2 — Масштаб + доведённая коммерция
**Цель:** настоящий магазин, держащий нагрузку. **Exit/SLA:** онлайн-продажи работают end-to-end; поиск/фасеты масштабируются; тяжёлые задачи асинхронны; 99.9% доступность.

### E2.1 Платежи 🔴 (см. `TZ_FIXES_MASTER` A-1)
- `lib/payment.ts` + `app/api/webhooks/payment/route.ts` (по §9/§14); провайдер (LiqPay/Mono/WayForPay); рассрочки `MONOBANK_PARTS`/`PRIVAT_PARTS`.
- Инициация в `createOrder` для онлайн-методов; идемпотентный webhook → `paymentStatus` lifecycle; сверка сумм.
- **Приёмка:** оплата картой/рассрочка проходят; webhook идемпотентен; неуспех/возврат корректны.

### E2.2 Доставка (Нова Пошта) 🔴
- Интеграция API НП: расчёт стоимости/сроков, выбор отделения/почтомата; заменить `shipping=0`; данные → `shippingDetails` (фид/schema).
- **Приёмка:** стоимость доставки реальная; выбор отделения; сроки в заказе.

### E2.3 Возвраты / RMA 🟡
- Модель `ReturnRequest` + флоу (заявка→одобрение→возврат средств); статусы; связь с `MerchantReturnPolicy`.
- **Приёмка:** клиент оформляет возврат; админ обрабатывает; статус виден.

### E2.4 Транзакционные письма 🟡 (часть уже есть `lib/email.ts`)
- Lifecycle: подтверждение заказа, смена статуса, отгрузка, возврат; через Resend; async (E2.6).
- **Приёмка:** письма уходят на ключевые события; сбой почты не валит заказ.

### E2.5 Фасеты → Algolia 🔴 (снимает потолок №1)
- Перенести фасеты/счётчики в Algolia (нативные reactive facets); убрать in-memory `take:50000` (`getCategoryFacets`); instant-search UI; полная переиндексация (bulk).
- **Приёмка:** категория >50k товаров без OOM; счётчики из движка; поиск мгновенный, опечатки/синонимы.

### E2.6 Очереди фоновых задач 🔴
- **Inngest / Upstash QStash** (serverless-friendly) или BullMQ+Redis: вынести в async — генерация фида Merchant, Algolia reindex, supplier sync, эмбеддинги RAG, письма, image-processing.
- Ретраи, идемпотентность, dead-letter.
- **Приёмка:** тяжёлые задачи не блокируют запросы; ретраи работают; есть мониторинг очереди.

### E2.7 Redis / Upstash (точечно) 🟡
- **Rate-limit ассистента** (`@upstash/ratelimit`) — заменить DB-скан в `api/assistant/chat`.
- Кэш вычислений (если нужен общий между инстансами) — иначе оставить `'use cache'`.
- **Приёмка:** rate-limit без скана БД; нет PII в лимитере.

### E2.8 Order Management (admin) 🟡
- Статус-машина `OrderStatus` с **guard'ами переходов** (сейчас принимается любой); массовые операции; экспорт; аудит (E1.4).
- **Приёмка:** недопустимый переход отклоняется; история статусов; bulk-операции.

---

# СТАДИЯ 3 — Дифференциация (ров — где рождается «лучший в сегменте») ⭐
**Цель:** уникальные фичи, которых нет у конкурентов. **Exit:** AI-подбор, B2B и инвентарь-хаб в проде; рост среднего чека и повторных продаж.

### E3.1 AI-советник v2 🌟 (см. `vision-next-level` Ставка 1)
- **Векторный RAG:** pgvector (расширение + поле `vector`), пайплайн `TechnicalDocument`→чанки→эмбеддинги→косинус-поиск; **реальные источники** вместо плейсхолдеров.
- **Tool-use** через официальный `@anthropic-ai/sdk` (инструменты: поиск/сток/совместимость) вместо дампа каталога.
- **Сборка комплекта (BOM):** «щит на N групп» → автоматы/УЗО/бокс + расчёт + «всё в корзину».
- **Калькуляторы:** сечение кабеля, номинал автомата, автономия ИБП (и как лид-магниты).
- **Приёмка:** ответы ссылаются на реальные документы; «собрать щит» даёт валидный комплект в корзину; калькуляторы корректны; регидратация из БД сохранена.

### E3.2 B2B по-взрослому 💼 (см. `vision-next-level` Ставка 2)
- Профиль юр.лица (ЄДРПОУ/реквизиты); **счёт с ПДВ / оплата по рахунку**; **RFQ «запрос прайса»** (воронка+статусы); **заказ списком/CSV по артикулам** + ре-ордер; **кредит-лимиты**; прайс-листы; опт-цены (`qty_breaks` довести до схемы).
- **Приёмка:** юр.лицо получает счёт с ПДВ; заказ по списку SKU формирует корзину; RFQ проходит цикл.

### E3.3 Инвентарь-хаб (ваш ров на базе robot-экосистемы) 🔗 (Ставка 3)
- Узаконить **supplier sync в приложении/сервисе** (мульти-поставщик); честная доступность «в наявності / під замовлення N днів»; связь ASKO/Prom/сайт.
- **Приёмка:** остатки поставщиков обновляются процессом приложения; карточка показывает срок поставки; меньше отмен «нет в наличии».

---

# СТАДИЯ 4 — Авторитет и доминирование
**Цель:** органический рост, повторные продажи, бренд-авторитет. **Exit/KPI:** ТОП-10 по ядру, dwell time 120+с, рост LTV/повторных, бренд-запросы +N%.

### E4.1 Контент-хаб / гайды 🟡 (двойное назначение SEO + RAG)
- Раздел `(content)/blog|guides`; гайды = корпус RAG (E3.1); AEO-формат (Direct Answer+FAQ через Content Factory). См. `TZ_SEO_TOP10` WP-5.
- **Приёмка:** ≥1 гайд/нед; индексируются; ассистент цитирует.

### E4.2 SEO-авторитет 🟡 (см. `TZ_SEO_TOP10`)
- Программные фасет-лендинги (WP-2), Schema-граф (WP-4), E-E-A-T/авторы (WP-6), бренд-сущность/Knowledge Panel (WP-8), линкбилдинг (WP-9), локальное SEO/GBP (WP-12).
- **Приёмка:** Rich Results зелёные; рост органики/брендовых; цитаты в AI Overviews.

### E4.3 Рекомендации / персонализация 🟡
- Recently viewed, related, AI-driven рекомендации, «с этим покупают»; персонализация выдачи.
- **Приёмка:** рост среднего чека/CTR карточек (A/B).

### E4.4 Аналитика спроса / маржи 🟡
- Дашборды: оборачиваемость, маржа, топ/аутсайдеры (продуктизировать идею `analyze-products`); GA4 события/конверсии (E1.3).
- **Приёмка:** управленческие решения на данных; видна маржа/velocity.

### E4.5 CRO / A/B-культура 🟢
- Инфраструктура A/B (см. `/ab-test-setup`), системные эксперименты на ключевых воронках.
- **Приёмка:** регулярные эксперименты; решения по данным, не по вкусу.

---

## Сводные KPI/SLA «топа»
- Доступность ≥99.9%; CWV зелёные (mobile p75); ошибки <0.1% сессий (Sentry).
- Покрытие тестами критпути ≥80%; CI-гейт; zero-downtime деплой.
- Конверсия/AOV/LTV растут (A/B-подтверждённо); ТОП-10 по ядру; повторные продажи растут.
- B2B-доля и средний чек профи растут; отмены «нет в наличии» <X%.

## Зависимости и последовательность
1. **Нельзя начинать 2 без 1** (тесты/пулинг/observability — страховка для всего дальнейшего).
2. E2.6 (очереди) разблокирует E3.1 (эмбеддинги), E2.5 (reindex), E2.1/E2.4 (вебхуки/письма).
3. E3.1 RAG ↔ E4.1 контент-хаб (один корпус).
4. Платежи/доставка (E2.1/E2.2) — до агрессивного маркетинга (E4).

## Команда и реализм (честно)
- Программа на **кварталы**, нужна команда (2–3 dev + QA) и дисциплина (тесты/ревью/мониторинг).
- Главный риск — **операционная зрелость** (Стадия 1), а не фичи. Стек bleeding-edge → тест-страховка обязательна.
- Двигаться итеративно: каждая стадия — самостоятельная ценность, релизить непрерывно.

## Трассировка
Долг/фиксы — `TZ_FIXES_MASTER`, `TZ_SEO_FIXES_ROUND2`; SEO — `TZ_SEO_TOP10`; Shopping — `TZ_GOOGLE_SHOPPING`+addendum; фичи по состоянию — `TZ_DEV_FEATURE_SPEC`; стратегия/ров — `_reversa_sdd/vision-next-level.md`; зрелость/оценка — `_reversa_sdd/qa-review-seo-shopping-2026-06-01.md`.
